(function($) {
    $.fn.turn = function(options) {
        console.log("turn.js is initialized with options:", options);
        this.css("border", "2px solid red"); // デバッグ用
        return this;
    };
})(jQuery);
